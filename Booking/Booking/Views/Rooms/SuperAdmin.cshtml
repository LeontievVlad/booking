@model IEnumerable<Booking.Models.ApplicationUser>

@{
    ViewBag.Title = "SuperAdmin";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
       

    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            
        </tr>
    }

</table>
<table class="table">
    <tr>
        
        <th>
            aaaaaaaaaa
        </th>

    </tr>

    @*@foreach (var item in ViewBag.allRoles)
    {
        <tr>
            <td>
                @item
            </td>

        </tr>
    }*@

</table>

@ViewBag.asdf